# AI Job Application Manager — Client

This directory contains the React frontend for the AI Job Application Manager.

## Setup

1. Run `npm install`
2. Run `npm start`

If your API is not running on `http://localhost:5000/api`, set `REACT_APP_API_URL`.
